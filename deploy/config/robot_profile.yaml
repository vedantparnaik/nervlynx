robot:
  id: "sr-3w-demo"
  site: "society-colony"
  mode: "surveillance"

sensors:
  cameras:
    count: 4
    fps: 15
  gps:
    enabled: true
  imu:
    enabled: true
  microphone:
    enabled: true
  speaker:
    enabled: true

comms:
  uplink: "wifi"
  hq_topic_prefix: "hq"
  telemetry_hz: 1

safety:
  watchdog_stale_after_s: 0.75
